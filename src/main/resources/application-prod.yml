spring:
  application:
    name: blog-query-service
  datasource:
    url: jdbc:mysql://3.80.230.96/command?allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234
  jpa:
    database: mysql
    open-in-view: false
    show-sql: true
    hibernate:
      properties:
        hibernate:
          format_sql: true
server:
  port: 8086
# port: 0

eureka:
  instance:
    #    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    instance-id: ${spring.application.name}:${spring.application.instance_id:${server.port}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://54.87.40.18:8761/eureka

env:
  user-feign-url : "http://44.201.129.31:8084/api/users/feign"
  post-command-feign-url : "http://54.147.162.65:8085/api/feign"
  file-feign-url: "http://3.86.230.148:8088/api/feign"